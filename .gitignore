# Windows / OneDrive noise
desktop.ini
Thumbs.db
ehthumbs.db
$RECYCLE.BIN/
*.lnk

# macOS, in case the folder ever travels
.DS_Store

# editor + scratch
.vscode/
.idea/
*.swp
*.tmp
*.bak
~$*

# nothing here needs a build step, but just in case
node_modules/
dist/
.env
.env.*

# reference file downloaded for the container-transform work, not my own code
container-transform-demo.html

# Source material for the site — CV originals, certificates, screenshots, screen
# recordings, photos. They stay on my machine; the published copies live in site/.
# Root level only, so site/ assets are unaffected.
/*.jpg
/*.jpeg
/*.png
/*.pdf
/*.docx
/*.mp4
